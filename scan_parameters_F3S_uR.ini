[options]
cleanOutput = True
runMadGraph = True
runPythia = False
runDelphes = False
ncpu = 1
ncore = 6
[AuxPars]
massPairs = $loop{[(1., 100.), (1., 500.), (1., 1000.), (1., 2000), (1., 3000.), (200., 500.), (200., 1000.), (200., 2000.), (200., 3000.), (400., 1000.), (400., 2000.), (400., 3000.), (900., 2000.), (900., 3000.)]}
[MadGraphPars]
#MG5 Cards:
proccard = Cards/DMSimp_F3S_uR/proc_card_1j.dat # Optional (only used if processFolder does not exist)
#paramcard = Cards/DMSimp_spin1/param_card_axial.dat # Path to the default parameter_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in paramcard. If not defined, the default file for the model will be used.
paramcard = Cards/DMSimp_F3S_uR/param_card.dat
runcard = Cards/DMSimp_F3S_uR/run_card_single_bias.dat # Path to the default run_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in runcard. If not defined, the default file for the model will be used.
delphescard = Cards/delphes_card_trimmed.dat # If not defined, will not run Delphes
pythia8card = Cards/pythia8_card.dat # If not defined it will not run Pythia and Delphes
#processFolder = ./Tests/DMSimp_axial_1j_gm_005 # Path to the folder to store the process generation output. If it already exists, the process generation will be #List of model parameters. Only values in this section are allowed as lists (for loops)
processFolder = DMSimp_DMSimp_F3S_uR_1j
[MadGraphSet]
nevents = 50000
MXs = ${AuxPars:massPairs}[0]
MYF3u1 = ${AuxPars:massPairs}[1]
lamF3u1x1 = 0.25
xqcut = 0.0
#xqcut = "%1.0f" %(max(30., max(2*${MXd}, ${MY1})/15.0))
run_tag = "yf3u1_%1.0f_%1.0f" %(${MYF3u1},${MXd})
