[options]
cleanOutput = True
runMadGraph = True
runPythia = True
runDelphes = True
ncpu = 1
ncore = 6
[AuxPars]
massPairs = $loop{[(300., 2500.)]}
[MadGraphPars]
#MG5 Cards:
proccard = Cards/DMSimp_spin1/proc_card_1j.dat # Optional (only used if processFolder does not exist)
#paramcard = Cards/DMSimp_spin1/param_card_axial.dat # Path to the default parameter_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in paramcard. If not defined, the default file for the model will be used.
paramcard = Cards/DMSimp_spin1/param_card_vector.dat
runcard = Cards/DMSimp_spin1/run_card_match_bias.dat # Path to the default run_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in runcard. If not defined, the default file for the model will be used.
delphescard = Cards/delphes_skimmed_ATLAS_13TeV_VF_Calorimeter-only.tcl # If not defined, will not run Delphes
pythia8card = Cards/pythia8_card_match.dat # If not defined it will not run Pythia and Delphes
#processFolder = ./Tests/DMSimp_axial_1j_gm_005 # Path to the folder to store the process generation output. If it already exists, the process generation will be #List of model parameters. Only values in this section are allowed as lists (for loops)
processFolder = DMSimp_vector_1j_atlas_VF_Cal-only
[MadGraphSet]
nevents = 50000
MXd = ${AuxPars:massPairs}[0]
MY1 = ${AuxPars:massPairs}[1]
#gAXd = 1.0
#gAd11 = ${AuxPars:gqValues}
#gAd22 = ${AuxPars:gqValues}
#gAd33 = ${AuxPars:gqValues}
#gAu11 = ${AuxPars:gqValues}
#gAu22 = ${AuxPars:gqValues}
#gAu33 = ${AuxPars:gqValues}
#xqcut = 0.0
xqcut = "%1.0f" %(max(30., max(2*${MXd}, ${MY1})/15.0))
#run_tag = "axial_%1.0f_%1.0f" %(${MY1},${MXd})
run_tag = "vector_%1.0f_%1.0f" %(${MY1},${MXd})
